import SectionTitle from '../../components/sectionTitle';

const Dinner = () => {
    return (
        <div>
            <SectionTitle title={'Dinner'} />
        </div>
    )
}

export default Dinner;