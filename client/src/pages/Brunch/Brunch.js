import SectionTitle from '../../components/sectionTitle';

const Brunch = () => {
    return (
        <div>
            <SectionTitle title={'Brunch'} />
        </div>
    )
}

export default Brunch;